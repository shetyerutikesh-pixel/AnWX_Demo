---
- name: Stop service on Linux
  hosts: "{{ deployment_host }}"
  gather_facts: no

  vars:
    service_name: nginx

  tasks:
    - name: Stop the service
      systemd:
        name: "{{ service_name }}"
        state: stopped
