---
- name: Start service on Linux
  hosts: "{{ deployment_host }}"
  become: yes
  gather_facts: no

  vars:
    service_name: nginx

  tasks:
    - name: Start the service
      systemd:
        name: "{{ service_name }}"
        state: started